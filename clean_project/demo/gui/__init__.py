"""
Optional Tkinter GUI for the Smart Waste Sorter demo.

Enable by setting 'gui.enabled: true' in 'config/demo_config.yaml'.
When disabled, the demo runs entirely in the terminal.

This package is self-contained: nothing else in the project imports from it.
Deleting the folder and removing the 'gui' section from the YAML file restores
the original terminal-only behaviour.
"""
