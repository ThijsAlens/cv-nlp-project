# src/

Contains the `waste_detector` Python package. Install it with `uv sync` from the
`model_training/` root, or run scripts directly with `uv run python scripts/<name>.py`
(the scripts add `src/` to `sys.path` automatically).

## Package

`waste_detector/` is the single installable package. See its README.md for a
breakdown of submodules.
