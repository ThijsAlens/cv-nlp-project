# src/

Contains the `nlp_assistant` Python package. Install with `uv sync` from the
project root, or run scripts directly with `uv run python scripts/<name>.py`
(the scripts add `src/` to `sys.path` automatically).
