# utils/

Shared file I/O helpers.

## Modules

| Module | Purpose |
|--------|---------|
| `io.py` | `read_json`, `read_yaml`, `read_text` -- all file reads in this package go through these helpers so error handling is consistent. |
